# Required for STRATIVY BRAIN board advisor API calls (Strativy backend).
MONEY_FLOW_GEMINI_API="your_strativy_brain_api_key_here"

# Optional: public URL when hosted (OAuth callbacks, self-referential links).
APP_URL="http://localhost:3000"
